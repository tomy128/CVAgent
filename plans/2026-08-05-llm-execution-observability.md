# Observable LLM Execution Implementation

1. Extend typed LLM settings and the Web form with reasoning effort and maximum output tokens.
2. Centralize `ChatOpenAI` construction and use it in workflow execution and representative structured connection testing.
3. Add timed node heartbeat events without changing node outputs or token streaming.
4. Render elapsed progress and slow-call guidance in the workbench.
5. Add regressions, validate with Ollama and the full suite, then commit code and archive the task separately.
